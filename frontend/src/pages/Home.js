import React from 'react';
import SiteHeader from '../components/SiteHeader';

const Home = (props) => {
    return (
        <div>
            <SiteHeader/>
        </div>
    );
};

export default Home;