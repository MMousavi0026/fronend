import React from 'react';
import SiteHeader from '../components/SiteHeader/SiteHeader';

const Home = (props) => {
    return (
        <div>
            <SiteHeader/>
        </div>
    );
};

export default Home;